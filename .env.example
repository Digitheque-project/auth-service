PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=sedra
DB_PASSWORD=sedra2003
DB_NAME=auth_service_db

JWT_SECRET=secret123

# Passe par la gateway (registre central des URLs de services : voir
# gateway/src/config/services.registry.ts) au lieu d'appeler user-services,
# service-service et chu-service en direct. Si l'un de ces services change
# d'URL ou est suspendu, seule la variable d'env de la gateway est à mettre
# à jour, pas celles-ci.
GATEWAY_URL=http://localhost:8080
INTERNAL_API_KEY=chu-internal-key-a1b2c3d4